import { Wrapper } from "./Logo.style";
import theme from "./../../../theme/theme";

const Logo = () => {
  return <Wrapper theme={theme} src="/assets/img/logo_secundary_color.svg" />;
};

export default Logo;
